# AutoMQ DevKit - Node Identity Template
# Variables replaced by generate-config:
#   ${NODE_ID}, ${CLUSTER_ID}, ${VOTERS}, ${BOOTSTRAP}

cluster.id=${CLUSTER_ID}
node.id=${NODE_ID}
controller.quorum.voters=${VOTERS}
controller.quorum.bootstrap.servers=${BOOTSTRAP}
